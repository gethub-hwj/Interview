问题: v-if和v-for哪个优先级高？如果两个同时出现，应该怎么优化得到更好的性能？ 
 答：v-for 的优先级高与v-if ，for在 循环的时候会每次都执行一次if ,每一次都需要遍历整个数组，影响速度。
    所以，有两种解决的办法。
    1.在请求玩数据的时候进行过滤。
    2.通过computed 进行过滤  this.arr.filter((x,y)=>{return xxxxxxx}) 这种方式  
